name: word-counter-tool
type: gradio-space
sdk: gradio

metadata:
  space_id: Chris4K/word-counter-tool
  original_url: https://huggingface.co/spaces/Chris4K/word-counter-tool

deploy:
  targets:
    - public-repo
    - huggingface

build:
  command: bash build.sh

test:
  command: echo "Tests not yet implemented"
